[assume a 1]
[assume b 1.5]
[assume c (/ 1 2)]
[assume d true]
[assume e "hello world"]
[assume f nil]
;[assume g null]

[predict (list 'some? a ': (some? a))]
[predict (list 'some? b ': (some? b))]
[predict (list 'some? c ': (some? c))]
[predict (list 'some? d ': (some? d))]
[predict (list 'some? e ': (some? e))]
[predict (list 'some? f ': (some? f))]
;[predict (list 'some? g ': (some? g))]
